# Instant Local Analysis

Open any `.py` file — CatalystOps immediately flags anti-patterns inline. No cluster, no setup.

**What it catches:** cartesian joins, Python UDFs, missing `.cache()`, unnecessary shuffles, schema mismatches, hardcoded credentials, and 25+ more.

Results appear as squiggles in the editor. Hover for a fix suggestion.
